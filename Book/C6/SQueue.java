/** Implements Queue based on stacks.
 *  @author Vingo
 */
import java.util.Stack;
public class Queue<T> {
    private T item;
    private int size;

    public Queue() {
        item = null;
        size = 0;
    }

    /** Adds an element to the end of the queue. */
    public void push() {

    }

    /** Pops the element of the front of the queue. */
    public T poll() {
        return null;
    }
}
